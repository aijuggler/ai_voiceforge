version: "3.8"
services:
  backend:
    container_name: ai_backend
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env
  frontend:
    container_name: ai_frontend
    depends_on:
      - backend
    build: ./frontend
    ports:
      - "3000:80"
    